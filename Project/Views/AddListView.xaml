<UserControl x:Class="Project.Views.AddListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Project.Views"
             Width="800" Height="950">
    <Grid>

        <!-- Фоновое изображение -->
        <Image Source="pack://application:,,,/Images/background.JPG" Stretch="Fill"/>

        <!-- Круглый логотип без белого фона -->
        <Ellipse Width="270" Height="270" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="580,-40,0,650">
            <Ellipse.Fill>
                <ImageBrush ImageSource="pack://application:,,,/Images/logo.png" Stretch="Uniform"/>
            </Ellipse.Fill>
        </Ellipse>
        
        <Button Content="Back to Main Menu" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="10" Click="BackButton_Click"/>
        
        <!-- Содержимое окна -->
        <ContentControl Name="MainContent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

        <!-- Кнопки для переключения контента -->
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,400,0,0" Orientation="Vertical">
            <Button Content="Add new list" Style="{StaticResource RoundedButton}"/>
        </StackPanel>
    </Grid>
</UserControl>

